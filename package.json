{
  "name": "openspot_2022",
  "version": "1.0.0",
  "description": "Minimal parking availability tracker (Node + MySQL + vanilla front-end).",
  "main": "server/index.js",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "dev": "NODE_ENV=development nodemon server/index.js",
    "lint": "echo \"(optional) add eslint if you want\"",
    "db:info": "echo \"Run: mysql -u root -p < db/schema.sql && mysql -u root -p openspot_2022 < db/seed.sql\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "mysql2": "^3.11.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
